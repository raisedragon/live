<?xml version="1.0" encoding="UTF-8" ?> 

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
  
<mapper namespace="com.raise.orgs.impl.persistence.entity.RightsEntity">

  <!-- USER INSERT -->

  <insert id="insertRights" parameterType="com.raise.orgs.impl.persistence.entity.RightsEntity">
    insert into ${prefix}rbac_rights (ID_, REV_, group_id_, resource_id_, type_)
    values (
      #{id ,jdbcType=VARCHAR},
      1,
      #{groupId ,jdbcType=VARCHAR},
      #{managedResourceId ,jdbcType=VARCHAR},
      #{type ,jdbcType=VARCHAR}
    )
  </insert>

  <!-- USER UPDATE -->

  <update id="updateRights" parameterType="com.raise.orgs.impl.persistence.entity.RightsEntity">
    update ${prefix}rbac_rights set
      REV_ = #{revisionNext ,jdbcType=INTEGER},
      group_id_ = #{groupId ,jdbcType=VARCHAR},
      resource_id_ = #{managedResourceId ,jdbcType=VARCHAR},
      type_ = #{type ,jdbcType=VARCHAR}
    where ID_ = #{id}
      and REV_ = #{revision}
  </update>
  
  <!-- USER DELETE -->

  <delete id="deleteRights" parameterType="com.raise.orgs.impl.persistence.entity.RightsEntity">
    delete from ${prefix}rbac_rights where ID_ = #{id} and REV_ = #{revision}
  </delete>

  <!-- USER RESULTMAP -->

  <resultMap id="rightsResultMap" type="com.raise.orgs.impl.persistence.entity.RightsEntity">
    <id property="id" column="ID_" jdbcType="VARCHAR" />
    <result property="revision" column="REV_" jdbcType="INTEGER" />
    <result property="groupId" column="group_id_" jdbcType="VARCHAR" />
    <result property="managedResourceId" column="resource_id_" jdbcType="VARCHAR" />
    <result property="type" column="type_" jdbcType="VARCHAR" />
  </resultMap>
  
  <!-- USER SELECT -->

  <select id="selectRightsById" parameterType="string" resultMap="rightsResultMap">
    select * from ${prefix}rbac_rights where ID_ = #{id,jdbcType=VARCHAR}
  </select>
    
</mapper>