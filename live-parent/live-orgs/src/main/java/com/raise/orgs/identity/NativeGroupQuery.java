package com.raise.orgs.identity;

import com.raise.orgs.query.NativeQuery;

/**
 * Allows querying of {@link com.raise.orgs.identity.Group}s via native (SQL) queries
 * @author Henry Yan(http://www.kafeitu.me)
 */
public interface NativeGroupQuery extends NativeQuery<NativeGroupQuery, Group> {

}